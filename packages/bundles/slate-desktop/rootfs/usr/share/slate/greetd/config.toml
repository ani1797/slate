[terminal]
vt = 1

[default_session]
# tuigreet lists the sessions the hyprland package already ships into
# /usr/share/wayland-sessions, so no custom session wrapper script is needed.
command = "tuigreet --remember --remember-session --time --sessions /usr/share/wayland-sessions"
user = "greeter"
