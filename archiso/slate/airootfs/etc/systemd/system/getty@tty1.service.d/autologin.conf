# Auto-login as root on tty1 for the live session, so it boots straight
# into a usable shell (and, via /etc/profile.d, into Hyprland).
[Service]
ExecStart=
ExecStart=-/usr/bin/agetty --autologin root --noreset --noclear - $TERM
