# Auto-login as the unprivileged slate user on tty1, so it boots straight
# into a usable shell (and, via /etc/profile.d, into Hyprland).
[Service]
ExecStart=
ExecStart=-/usr/bin/agetty --autologin slate --noreset --noclear - $TERM
